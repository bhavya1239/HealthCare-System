spring:
  jpa:
    show-sql:  true
    hibernate:
        ddl-auto:  create # do create for first time then update it
  datasource:
    url: jdbc:mysql://localhost:3306/hc1
    username:  root
    password:  mysql
    driver-class-name:  com.mysql.cj.jdbc.Driver
